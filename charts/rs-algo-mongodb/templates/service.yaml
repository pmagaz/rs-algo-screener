apiVersion: v1
kind: Service
metadata:
  name: {{ include "rs-algo-mongodb.fullname" . }}
  labels:
    app: {{ .Values.service.metadata.labels.app }}
    {{- include "rs-algo-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rs-algo-mongodb.selectorLabels" . | nindent 4 }}
