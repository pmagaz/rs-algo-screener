apiVersion: v1
kind: Service
metadata:
  name: {{ include "rs-algo-screener.fullname" . }}
  labels:
    app: {{ .Values.service.metadata.labels.app }}
    {{- include "rs-algo-screener.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rs-algo-screener.selectorLabels" . | nindent 4 }}
